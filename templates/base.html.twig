<!doctype html>
<html lang=fr>
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="{{ asset('style.css') }}">
    {% block title %}
        <title>The Big Library</title>
    {% endblock %}
</head>
<body>
{% for message in app.flashes('success') %}
    <div class="alert alert-success">
        <h2 class="alert-success text-center">{{ message }}</h2>
    </div>
{% endfor %}
<header>
    <nav>
        <ul>
            <li><a href="{{ path('home') }}">Accueil</a></li>
            <li><a href="{{ path('admin-authors') }}">Auteurs</a></li>
            <li><a href="{{ path('admin-books') }}">Bouquins</a></li>
            <li><a href="{{ path('new-author') }}">Ajouter un auteur</a></li>
            <li><a href="{{ path('new-book') }}">Ajouter un livre</a></li>
            <li><form action="{{ path('admin-authors-search') }}" method="get">
                    <input type="text" name="search" placeholder="Votre recherche">
                    <input type="submit">
                </form>
            </li>
        </ul>
    </nav>
</header>
<main>


{% block main %}

{% endblock %}
</main>
<footer class="footer">
    <ul>
        <li><a href="{{ path('home') }}">Accueil</a></li>
        <li><a href="{{ path('admin-authors') }}">Auteurs</a></li>
        <li><a href="{{ path('admin-books') }}">Bouquins</a></li>
        <li><a href="{{ path('new-author') }}">Ajouter un auteur</a></li>
        <li><a href="{{ path('new-book') }}">Ajouter un livre</a></li>
    </ul>
</footer>
</body>
</html>